#ifndef COMMONHPP
#define COMMONHPP

#include <iostream>
using std::cout;
using std::endl;

#define FOR(i,max)		for(int i=0;	i<(max);i++)
#define FOR2(i,min,max) for(int i=(min);i<(max);i++)

#endif//COMMONHPP